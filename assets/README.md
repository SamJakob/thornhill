# Thornhill System Assets
This represents assets made available to the system at build time, such as the boot logo.

## `root/`
Any content in the `root/` directory will be copied into the root of the created system
image.
